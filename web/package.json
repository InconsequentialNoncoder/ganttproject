{
  "name": "ganttproject-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare-native web version of GanttProject (MVP foundation)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.worker.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@types/node": "^22.19.19",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.84.0"
  }
}
